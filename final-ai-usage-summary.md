# Final AI Usage Summary

Primary tool: **Cursor**.

Used across the lifecycle:

1. **Planning** — broke the assessment brief into Core-only phases and repo structure  
2. **Design** — data model, API contract samples, UI flow  
3. **Implementation** — models, migrations, Django/DRF routes, React screens, FE↔BE wiring  
4. **Testing** — pytest state-machine integration tests  
5. **Debugging** — missing env vars, DB path issues, relationship simplification  
6. **Review / docs** — onboarding READMEs, Swagger/`api-contract.md`, reflection  

Judgment: accepted readable Core solutions; rejected Stretch overkill (Docker/Postgres) and overbuilt abstractions. Prompt notes live in `ai-prompts/`.
